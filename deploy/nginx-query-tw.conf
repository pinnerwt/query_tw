# Drop-in vhost for query.tw -> cuizhao-api:8080.
# Installed by deploy/deploy-monitor.sh into the existing nginx running
# from /home/ubuntu/tiayn-v2/deploy/nginx.conf. Self-signed cert is fine
# because Cloudflare proxies query.tw and accepts self-signed origins.

# Marker line so the installer can locate and replace this block on
# subsequent deploys without duplicating it.
# CUIZHAO-NGINX-BLOCK-START
upstream cuizhao_api {
    server cuizhao-api:8080;
}

server {
    listen 80;
    listen 443 ssl;
    server_name query.tw www.query.tw;

    ssl_certificate /etc/nginx/ssl/server.crt;
    ssl_certificate_key /etc/nginx/ssl/server.key;

    client_max_body_size 4m;

    location / {
        proxy_pass http://cuizhao_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}
# CUIZHAO-NGINX-BLOCK-END
