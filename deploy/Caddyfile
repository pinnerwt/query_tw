# Caddyfile for query.tw deployment.
# This file is consumed by the candy compose stack on the monitor host;
# see deploy/deploy-monitor.sh for usage.
query.tw {
  encode gzip zstd
  reverse_proxy api:8080
}
